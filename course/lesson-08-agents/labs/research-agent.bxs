// research-agent.bxs
/**
 * LAB: Research Assistant Agent
 *
 * OBJECTIVE:
 * Build an agent that researches topics and creates structured reports
 *
 * REQUIREMENTS:
 * 1. Create at least 2 research tools (search, fact-check, etc.)
 * 2. Agent should plan multi-step research process
 * 3. Generate structured report with sections
 * 4. Handle missing or unavailable information
 * 5. Cite sources for findings
 *
 * BONUS CHALLENGES:
 * - Add web scraping tool (simulated)
 * - Implement source credibility scoring
 * - Generate reports in different formats (markdown, JSON, etc.)
 * - Add report export to file
 * - Implement research caching to avoid duplicate searches
 *
 * EXAMPLE OUTPUT:
 * Agent: Researching "Machine Learning"...
 * Agent: [uses search tool]
 * Agent: [uses fact-check tool]
 * Agent: Generating report...
 *
 * REPORT: Machine Learning
 * ======================
 * Definition: ...
 * Key Concepts: ...
 * Applications: ...
 * Sources: ...
 *
 * HINTS:
 * - Define tools with aiTool()
 * - Create agent with aiAgent()
 * - Set clear instructions for research process
 * - Use structured output in prompts
 */

println( "=== Research Assistant Agent Lab ===" )
println()

// YOUR CODE HERE
//
// Step 1: Create research tools
// searchTool = aiTool()
//     .setName( "search" )
//     .setDescription( "Search for information" )
//     .setHandler( ( args ) => { /* search logic */ } )
//
// Step 2: Create agent
// agent = aiAgent()
//     .setName( "Researcher" )
//     .setInstructions( "Research thoroughly..." )
//     .setTools( [ searchTool ] )
//
// Step 3: Run research
// report = agent.run( "Research topic X" )

println( "💡 Implement your research agent above!" )
