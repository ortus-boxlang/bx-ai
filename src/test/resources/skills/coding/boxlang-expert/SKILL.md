---
name: boxlang-expert
description: BoxLang language rules and idioms. Use when writing or reviewing BoxLang code.
---
Use `char(10)` not `chr(10)`. Use `castAs` not `javaCast()`.
Imports must be at the top of the class, never inline inside methods.
